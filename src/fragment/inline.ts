/** The fragment prelude: an IIFE leaving `__LIVE_PREVIEW_FRAGMENT__`, emitted only for pages with a fragment endpoint. */
export { createFragmentStrategy } from './index';
export { createRouteStrategy } from './route';
